<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= current_user.admin? %>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
